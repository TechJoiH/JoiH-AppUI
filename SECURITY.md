# 安全策略

## 支持范围

当前仅维护最新的 `0.x` 预发布版本。预发布 API 可能变化，但资源释放、输入阻挡、线程回送和构建边界中的安全问题会优先处理。

## 报告问题

请不要在公开 Issue 中披露可利用细节、凭据或真实项目数据。仓库公开并启用 GitHub Security Advisories 后，请使用仓库的 **Security > Report a vulnerability** 私密报告入口。

报告应尽量包含：

- 受影响版本与 Unity 版本；
- 最小复现步骤；
- 影响范围；
- 日志或示例中已脱敏的证据；
- 已知缓解方式。

在正式安全联系渠道启用前，不应把敏感信息写入仓库、Issue 或 Pull Request。
